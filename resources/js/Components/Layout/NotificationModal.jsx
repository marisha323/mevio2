import {useEffect, useState} from "react";


export const NotificationModal = ({isVisible, invitations}) => {

    return(
        <div className={`notification-modal ${isVisible ? 'notification-modal-visible' : ''}`}>

        </div>
    )
}
